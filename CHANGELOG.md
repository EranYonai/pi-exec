# Changelog

## [0.1.0] — unreleased

- Added the portable one-command generation, parsing, safety-lint, and history core.
- Added pi CLI/TUI integration with terminal confirmation, dry-run, streaming execution,
  timeout/abort handling, help, history preview, and mode-correct output.
- Use pi's active model and authentication; pi's native `--model` handles one-off choices.
- Added the short `scripts/pi-exec.sh` launcher.
- Fixed terminal confirmation keeping pi alive after completion.
